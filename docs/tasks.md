# 项目任务清单

本文件是项目进度的统一记录。学习目标见 [需求文档](requirements.md)，技术与 Pattern 顺序见 [技术学习路线](technical-roadmap.md)。

## 当前进度

- 当前阶段：SDK 扩展 / E06-A 本地技能发现与按需加载已实现；4 项测试通过，1 项真实符号链接测试因环境权限跳过（2026-09-23）。
- 下一步：回顾 E06-A 的目录、正文和资源加载过程，再接入 E06-B 模型选择；真实符号链接越界验证保留待办。E05 回顾已提供，理解未单独确认；E02 精确计数、E03 工具轨迹及未确认体验仍保留待办；日志问题暂不扩展。
- 路线调整：继续 SDK，扩展计划已写入 docs/sdk-learning-plan.md；P4 收尾后从 E01-A 进程内会话开始，暂缓框架迁移。
- 验证记录：此前受控成功响应与空响应验证通过；用户随后改为模块级配置实例，配置错误现在由 Pydantic 在导入时直接报告。真实回复原文未提供，不补写实测内容。
- 代码组织：Schema 已按功能集中到 schemas/config.py 与 schemas/tools.py，业务模块仅导入使用；所有配置实例集中在 config/settings.py 初始化并记录日志，不按配置类型拆分初始化模块。
- 工具复用：P0 与 P1 的 add 共用 tools/arithmetic.py，工具 Schema 继续由 schemas/tools.py 管理。
- 工具调用进度：add 已实现；2026-09-13 五项离线测试通过，真实调用返回 485。当前最多执行一个工具、发送两次请求。

## 维护方式

- 2026-09-23：按用户建议将 MCP 声明转换封装进 Pydantic 包装类 `MCPTool`，通过 `tool.model_tool()` 获取模型工具声明；5 项 MCP 相关测试通过，学习阶段未推进。

- 2026-09-22：修复 E04 请求调用的 Pylance 类型报错，为响应格式和完整请求参数添加 SDK 类型标注。两个相关文件的 Pyright 检查为 0 错误、0 警告，E04 的 4 项离线测试通过；未推进学习阶段。

- 2026-09-18：按用户要求将 `tests/manual_logly_json_bad_case.py` 简化为普通脚本，移除 unittest 和子进程；用 print 展示原文、logly 展示实际输出。运行正常退出，仍复现数组和方括号内容丢失；不再使用断言或失败退出码。

- 2026-09-18：新增 `tests/manual_logly_json_bad_case.py` 最小断言反例，独立子进程调用 `logger.info("payload=\n{}", json.dumps(..., indent=2))`，显式 stdout sink 且 colorize=False。实测 1 项失败、退出码 1：messages 数组及字符串中的 `[hello]` 均丢失。测试断言正确行为，不将当前缺陷作为通过条件；独立运行，不加入默认测试发现。未修复业务代码。

- 2026-09-18：暂停学习步骤推进，新增 `tests/manual_logly_json.py` 独立复现脚本。logly 0.2.2 实测：占位符传入 JSON 的默认文本输出完整；f-string JSON 触发 KeyError；bind 字典配合 serialize/pretty_json 时 payload 被字符串化且嵌套列表内容消失。仅记录复现，未修改业务日志或加入 PII 处理。

- 2026-09-14：按现有代码更新 README，补齐 P0–P4、E01-A 入口、配置、验证记录和文档导航；本次仅维护文档，学习阶段未推进。

- `[ ]` 表示未完成，`[x]` 表示已完成；正在进行的任务在文字后标注“进行中”。
- 开始工作前查看当前进度；完成任务后更新勾选项、当前阶段和下一步。
- 每次只推进当前约定的步骤，后续任务在开始前再细化。
- 代码完成不等于学习项完成；完成验证并记录结果后，才勾选整个学习项。
- 不把尚未运行的代码、未做的验证或计划中的文档标记为完成。

## 项目准备

- [x] 使用 uv 初始化 Python 3.14 项目。
- [x] 初始化 Git 并提交项目基础文件。
- [x] 编写需求文档。
- [x] 确定四阶段技术路线与 Pattern List。
- [x] 创建 AGENTS.md，记录需求文档阅读要求及编码规则。
- [x] 创建四条技术路线的 P0–P4 目录，以及案例、测试和文档目录。
- [x] 建立任务清单与进度维护约定。

## 第一阶段：SDK 手写

### P0：模型调用与工具调用

- [x] 编写 `docs/patterns/p00_basics.md` 的模型调用部分：概念、实验目标、执行流程和验收标准。
- [x] 确定接口、SDK 与初期交互方式：OpenAI 兼容 Chat Completions、openai Python SDK、命令行固定输入；具体服务与模型改为运行时配置（2026-09-13）。
- [x] 配置依赖与认证信息管理，使用 logly 记录运行信息。
  - [x] 实现 LLMConfig，使用 .env 与 pydantic-settings 加载和校验配置，提供 .env.example。
  - [x] 接入模型 SDK 与 logly。
- [x] 实现单次模型调用。
- [x] 运行验证，解释请求与响应，补充真实运行说明和实验结果。
- [x] 补充 P0 工具调用学习文档，确定 add 工具与加法示例。
- [x] 实现工具定义、调用识别、参数解析、函数执行和结果回传。
- [x] 验证完整工具调用链，记录结果与问题，完成 P0 学习总结。

### P1：ReAct 风格循环

- [x] 编写学习文档，明确任务、循环流程与停止条件。
- [x] 实现消息管理、连续工具调用、正常结束与步数上限。
- [x] 加入工具名称与参数校验、错误处理和执行记录。
- [x] 验证正常与异常路径，补充实验结果并总结。

### P2：Plan-and-Execute

- [x] 编写学习文档，确定计划结构、执行方式与调整条件。
- [x] 实现规划、执行和执行前的有限计划修正。
- [x] 验证并与 P1 对比，记录行为、调用次数、耗时与失败情况。

### P3：Reflection / Critique

- [x] 编写学习文档，明确检查标准与修改次数限制。
- [x] 实现初始结果生成、检查反馈和修改过程。
- [x] 按实际验收标准验证结果，记录改进情况与额外成本。

### P4：多 Agent 协作

- [x] 确定首个协作模式、角色、信息传递与结束条件，编写学习文档。
- [x] 实现多 Agent 分工与交接。
- [x] 验证并与单 Agent 版本对比，记录收益与复杂度。

## SDK 扩展：E01–E13

顺序、最小实验与验收标准见 [SDK 扩展学习计划](sdk-learning-plan.md)。各子步骤按实际验证更新，整个 E01 尚未完成。

- [x] 制定 SDK 扩展计划并同步需求与路线。
- [ ] E01 Memory：进程内会话 → 历史持久化 → 明确偏好更新与删除。
  - [x] E01-A：实现进程内文本会话，验证历史传递、隔离、失败处理及真实固定示例。
  - [ ] E01-A：交互体验与学习回顾。
  - [ ] E01-B：历史持久化（实现、离线验证与真实模型重启验证完成，学习理解待确认）。
    - [x] 按会话保存/加载 JSON，校验历史，保存失败不提交本轮。
    - [x] 离线验证跨进程恢复、隔离、文件损坏与失败处理；全套测试通过（2026-09-15）。
    - [x] 真实模型重启追问与新会话隔离（2026-09-16，通过交互入口管道输入，非用户手动体验）。
    - [ ] 学习回顾理解确认。
  - [ ] E01-C：明确偏好更新与删除（进行中）。
    - [x] 实现独立偏好档案，支持显式设置、更新、删除、查看和携带当前偏好提问。
    - [x] 新增 4 项离线测试，全套测试通过（2026-09-16）。
    - [x] 真实模型验证：设置后回答青绿色、更新后回答紫色、删除后回答不知道（2026-09-16）。
    - [ ] 学习回顾理解确认。
- [ ] E02 上下文管理：全量、窗口、摘要与输入预算。
  - [x] E02-A：实现同一份预置历史的全量/最近 N 轮对照，逐条展示请求消息。
  - [x] E02-A：新增 6 项离线测试，全套 66 项通过（2026-09-16）。
  - [x] E02-A：真实对照，全量回答青绿色，窗口回答不知道；分别发送 10/6 条消息（2026-09-16）。
  - [ ] E02-A：学习回顾与手动体验。
  - [ ] E02-B：历史摘要（实现完成，回顾进行中）。
    - [x] 仅摘要窗口外历史，组合摘要与最近完整轮次；保留 E02-A 入口。
    - [x] 新增 5 项离线测试，全套 71 项通过（2026-09-17），包含受控遗漏与失真。
    - [x] 真实对照：全量/摘要回答青绿色，窗口回答不知道；观察到摘要把已回答加法标为未完成，最终正文比全量长 15 字符（2026-09-17）。
    - [ ] 学习回顾与手动体验。
  - [ ] E02-C：输入预算与输出预留（本地估算实验进行中）。
    - [x] 实现本地估算、按完整文本轮次裁剪、固定输入超预算拒绝；发送 max_tokens 输出限制。
    - [x] 新增 6 项离线测试，全套 77 项通过（2026-09-17）。
    - [x] 真实验证：总预算 640 裁至 3 轮并回答不知道，1000 保留 4 轮并回答青绿色；总预算 200 的预览在请求前拒绝（2026-09-17）。
    - [ ] 学习回顾；精确模型计数待后续。
  - [x] E02 工具历史：按完整用户轮次裁剪；调用与结果 ID 校验，参数和结果均计入估算；新增 6 项测试，全套 83 项通过（2026-09-18）。
  - [x] E02 工具历史真实对照：小预算移除工具轮次并回答无记录，大预算保留完整轮次并回答 5 和 9；不重跑历史工具。
- [ ] E03 评估与执行追踪：固定案例、独立验收与比较记录。
  - [x] E03-A：六题固定 E01 案例，规则判分，记录完成状态、实际/预期答案、请求尝试数、耗时和错误类型。
  - [x] 新增 3 项评估器测试，全套 86 项通过（2026-09-18）。
  - [x] E03-A：真实运行 gemini-2.5-flash，完成 6/6、规则通过 6/6，六次请求、零次工具执行（2026-09-18）。
  - [ ] E03-A：学习回顾。
  - [ ] P1/P3 工具轨迹与单变量对照，后续按学习需要开展。
- [ ] E04 结构化输出：服务约束与本地校验对照。
  - [x] 最小两次请求对照，两种模式均校验原始响应并单独检查事实。
  - [x] 新增 4 项测试，全套 90 项通过（2026-09-22）。
  - [x] 真实服务两模式均通过结构校验并提取青绿色（2026-09-22）；不据此声称网关必然执行严格约束。
  - [ ] 学习回顾。
- [ ] E05 MCP：本地服务、工具发现与调用。
  - [x] E05-A：本地 stdio 服务复用 add，客户端完成初始化、工具发现与调用，真实返回 485.0（2026-09-23）。
  - [x] 新增 1 项子进程集成测试，覆盖正常调用、未知工具、无效参数及错误后继续调用；全套 91 项测试通过。
  - [x] 模型工具定义适配与结果回传；新增 4 项受控测试，全套 95 项通过（2026-09-23）。
  - [x] 真实模型接入验证：gemini-2.5-flash 请求 add，MCP 执行成功，模型最终回答 485；两次模型请求、一次工具调用（2026-09-23）。
  - [x] 真实子进程故障验证：工具函数抛出异常返回 isError，同一会话可继续调用；执行中服务退出使客户端抛出 CONNECTION_CLOSED。新增 2 项集成测试，7 项 MCP 相关测试通过（2026-09-23）。
  - [ ] E05-A 学习回顾与理解确认。
- [ ] E06 Skill：技能发现、选择、按需加载与权限边界。
  - [x] 概念与最小实验设计，见 docs/e06_skill.md（2026-09-23）。
  - [x] E06-A：本地技能发现、说明与资源按需加载、未知名称及绝对路径/父目录跳转拒绝；4 项测试通过，本地两个技能均可运行。
  - [ ] E06-A：真实符号链接越界验证（当前环境无法创建符号链接，1 项测试跳过）；学习回顾。
  - [ ] E06-B：模型选择、上下文与工具权限验证、真实体验及回顾。
- [ ] E07 人工介入：批准、拒绝、修改后重新确认。
- [ ] E08 工具可靠性：超时、有限重试、幂等与结果查询。
- [ ] E09 状态保存与恢复：检查点、未决操作与审批状态。
- [ ] E10 Sandbox：隔离代码执行及文件、网络、资源边界验证。
- [ ] E11 检索与 RAG：本地资料、引用、无依据回答与向量检索对照。
- [ ] E12 提示注入：文档、工具、技能资源与代码授权边界。
- [ ] E13 流式响应与取消：增量、阶段事件、停止与副作用状态。
- [ ] 完成 SDK 总回顾，选择后续框架的扩展对照项。

## 后续框架阶段

以下每个学习项均包含：阅读已有 Pattern 文档、实现、运行相同验收任务、补充运行说明和框架对比记录。进入对应阶段时再拆分具体任务。

### 第二阶段：Pydantic AI

- [ ] P0：模型调用与工具调用。
- [ ] P1：ReAct 风格循环。
- [ ] P2：Plan-and-Execute。
- [ ] P3：Reflection / Critique。
- [ ] P4：多 Agent 协作。
- [ ] 汇总与 SDK 手写版本的差异。

### 第三阶段：LangChain

- [ ] P0：模型调用与工具调用。
- [ ] P1：ReAct 风格循环。
- [ ] P2：Plan-and-Execute。
- [ ] P3：Reflection / Critique。
- [ ] P4：多 Agent 协作。
- [ ] 汇总与已有实现的差异。

### 第四阶段：LangGraph

- [ ] P0：模型调用与工具调用。
- [ ] P1：ReAct 风格循环。
- [ ] P2：Plan-and-Execute。
- [ ] P3：Reflection / Critique。
- [ ] P4：多 Agent 协作。
- [ ] 汇总四条技术路线的收益、限制与适用场景。

## P1 本次验证记录（2026-09-13）

- 全部 13 项离线测试通过（含 P0）。
- 真实模型连续调用 add 两次，得到 485、581，第 3 步正常回答并结束。
- P1 学习回顾完成：用户理解调用次数、最大步数与前后依赖，已补充同轮调用参数预先确定的区别。

## P2 本次验证记录（2026-09-13）

- 全部 20 项离线测试通过（含 P0/P1）。
- 真实模型首次生成合法依赖计划，执行两次 add，最终得到 581；模型请求 2 次，耗时 3.29 秒，无修正。
- 与 P1 的调用次数、依赖传递和失败策略已记录在 P2 文档。

## P2 学习回顾（2026-09-14）

- 已回顾 step-id 依赖引用、执行前再次调用模型修正无效计划，以及结构合法但参数不符合任务的语义错误。
- 用户明确指出：不能保证每一步都是幂等的，重跑整个计划可能重复付款，因此执行失败后不能盲目重跑。
- P2 学习回顾完成；本次未重新运行测试或真实模型调用。

## P3 实现与离线验证（2026-09-14）

- 用户理解检查者需要原始任务与工具记录作为依据。
- 已实现固定计算、初稿、结构化检查、有限修改和复查；默认最多修改一次，修改不重跑工具。
- 新增 9 项 P3 测试，全套 29 项离线测试通过。覆盖直接通过、修改成功、额度耗尽、零修改、非法反馈、空响应、请求失败、工具失败和检查误判。
- P2/P3 共用文本请求函数，提取至 tools/llm.py；P2 回归测试通过。
- 本次未调用真实模型，不将受控响应记为真实模型改进效果。

## P3 响应格式兼容修复（2026-09-14）

- 用户真实运行遇到检查反馈解析失败；共用响应函数现移除开头完整的 think 块，P2/P3 的 JSON 校验前移除外围代码围栏。
- 不补造缺失字段值；如 `"issues": }` 仍明确报错，通过时应返回 `"issues": []`。
- 新增 6 项测试，全套 35 项离线测试通过，覆盖 think、JSON 围栏、空正文、未闭合标签和缺失 JSON 值。真实模型修复后重跑待完成。
- 按用户要求将 think 与 JSON 围栏清理完全封装在 request_text 内部，P2/P3 仅调用 request_text 并使用各自 Pydantic 模型校验，无需显式 unwrap。全套 37 项离线测试通过。

## P3 真实运行与回顾（2026-09-14，用户提供日志）

- 错误工具记录为 486、583。一次运行修改后仍错误，达到 revision_limit：4 次模型请求、2 次工具执行、10.19 秒。
- 用户反馈增加修改额度仍未修好；更换为其认为更强的模型后，错误答案被检查者放行：2 次模型请求、2 次工具执行、19.43 秒。实际验收失败，原任务正确结果为 581。
- 模型具体名称与增加后的额度未提供，不据此推断模型整体能力或性能差异。日志中 issues 内容显示不完整，不补写检查细节。
- 学习结论：检查通过不等于事实正确；错误工具依据可能导致修正失败或误判通过，增加额度与更换模型均不保证改进。P3 回顾完成。

## P4 实现与离线验证（2026-09-14）

- 协调者生成委派并汇总，计算者独立维护消息历史，仅能调用 add；失败返回原因与已完成记录，不自动重跑。
- 计算者请求上限通过 COOP_WORKER_MAX_STEPS 配置，默认 5；正常受控路径共 5 次模型请求、2 次工具执行，结果 485、581。
- 新增 10 项 P4 测试，全套 47 项离线测试通过，涵盖上下文和工具权限、非法委派与调用、失败传播、请求限制、响应完整性、包装清理和错误工具依据。
- 本次未调用真实模型，真实耗时、对比与学习回顾待完成。

## P4 真实对比验证（2026-09-14）

使用当前配置的 gemini-2.5-flash 和同一 OpenAI 兼容服务，分别运行 P4 与 P1；输入均为 P4 的 TASK，使用相同 add 实现。

| 版本 | 模型请求 | 工具执行 | 耗时 | 事实验收 |
| --- | --- | --- | --- | --- |
| P4 | 协调者 2 次、计算者 3 次，共 5 次 | 2 次 | 16.93 秒 | 两步算式 127 + 358 = 485、485 + 96 = 581，最终结果 581 |
| P1 | 3 次 | 2 次 | 6.71 秒 | 两步算式与最终结果均正确 |

P4 正常完成一次委派与返回，P1 正常结束循环。P4 多出委派和汇总两次请求；单次耗时不代表稳定性能，未统计 token 或费用。收益是明确职责、上下文和工具权限边界，本例未观察到准确性收益。不同角色可以使用同一个模型，由系统提示词、独立消息历史和代码限制的工具权限区分。保留原始任务可以减少转述遗漏；计算失败就返回原因和已有记录，不补猜答案或自动重跑。

本次仅运行真实示例和更新文档，未修改业务代码、未重跑离线测试。学习回顾已提供，用户理解待确认；未开始 E01。
